# Copier ce fichier en .env et renseigner la cle
ANTHROPIC_API_KEY=sk-ant-...

# Port du serveur Express
PORT=3000

# Mode LLM simule (true = pas d'appel reel a l'API, reponses factices)
# Utile pour rejouer le benchmark sans consommer de credits.
SIMULATE_LLM=false
