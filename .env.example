# Copier ce fichier en .env et renseigner la clé
ANTHROPIC_API_KEY=sk-ant-...

# Port du serveur Express
PORT=3000

# Mode LLM simulé (true = pas d'appel réel à l'API, réponses factices)
# Utile pour rejouer le benchmark sans consommer de crédits.
SIMULATE_LLM=false
